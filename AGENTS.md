# BenchKit

For the full benchkit skill, see [`skills/benchkit/SKILL.md`](skills/benchkit/SKILL.md).

To install the skill into a project: `benchkit init`
To install globally: `benchkit install-skill`
